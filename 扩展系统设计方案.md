# 扩展系统设计方案

## 一、目标

为当前应用增加扩展能力。应用启动时扫描指定扩展目录，读取每个扩展的配置文件并完成初始化；所有成功载入的扩展统一写入全局缓存，供主线程、工作区会话、Studio 管理端以及后续消息路由使用。

扩展系统需要支持：

- 启动时扫描扩展目录
- 读取扩展配置
- 根据配置启动扩展能力
- 把已载入扩展放入全局缓存
- 主线程通过 Studio 与扩展通信
- 扩展可声明登录注册 HTTP 接口
- 扩展可声明可接收的 HTTP 推送 type
- 扩展与主线程之间统一使用 JSON 通信协议

---

## 二、目录结构建议

默认扩展目录建议为：

```text
extensions/
  example-extension/
    extension.yaml
    README.md
    assets/
    bin/
    scripts/
```

也可以通过服务端配置指定扩展目录：

```yaml
extension:
  enabled: true
  dir: ./extensions
```

应用启动时读取 `extension.dir`，遍历其一级子目录。每个子目录如果存在 `extension.yaml`，则认为是一个扩展候选。

---

## 三、扩展配置文件

扩展配置文件固定命名为：

```text
extension.yaml
```

示例：

```yaml
id: wechat
name: 微信接入扩展
version: 1.0.0
description: 提供微信登录、注册、消息推送与 Studio 通信能力
enabled: true

entry:
  type: http
  baseUrl: http://127.0.0.1:19001
  healthPath: /health

studio:
  enabled: true
  channel: wechat

http:
  login:
    enabled: true
    path: /api/wechat/login
    method: POST
  register:
    enabled: true
    path: /api/wechat/register
    method: POST
  push:
    enabled: true
    path: /api/wechat/push
    method: POST
    types:
      - chat.message
      - chat.event
      - user.login
      - user.register
      - workspace.bind

permissions:
  - user.read
  - user.write
  - chat.push
  - studio.message

metadata:
  author: lls
  homepage: https://example.com
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | 字符串 | 是 | 扩展唯一标识，全局唯一 |
| `name` | 字符串 | 是 | 扩展展示名称 |
| `version` | 字符串 | 是 | 扩展版本 |
| `description` | 字符串 | 否 | 扩展描述 |
| `enabled` | 布尔 | 否 | 是否启用，默认 true |
| `entry.type` | 字符串 | 是 | 扩展通信类型，首期建议支持 http |
| `entry.baseUrl` | 字符串 | http 类型必填 | 扩展服务地址 |
| `entry.healthPath` | 字符串 | 否 | 健康检查路径 |
| `studio.enabled` | 布尔 | 否 | 是否允许 Studio 通信 |
| `studio.channel` | 字符串 | 否 | Studio 通信通道名 |
| `http.login` | 对象 | 否 | 登录 HTTP 接口配置 |
| `http.register` | 对象 | 否 | 注册 HTTP 接口配置 |
| `http.push` | 对象 | 否 | HTTP 推送配置 |
| `permissions` | 字符串数组 | 否 | 扩展权限声明 |
| `metadata` | 对象 | 否 | 扩展自定义元数据 |

---

## 四、应用启动加载流程

启动流程建议如下：

```text
应用启动
  ↓
读取服务端配置 extension.enabled / extension.dir
  ↓
扫描扩展目录一级子目录
  ↓
查找 extension.yaml
  ↓
解析并校验扩展配置
  ↓
检查 enabled 状态
  ↓
执行健康检查或初始化握手
  ↓
写入全局扩展缓存
  ↓
主线程和 Studio 可通过缓存访问扩展
```

伪代码：

```go
func LoadExtensions(dir string) error {
    items := scanDir(dir)
    for _, item := range items {
        configPath := path.Join(item.Path, "extension.yaml")
        if !exists(configPath) {
            continue
        }

        extConfig := parseYaml(configPath)
        if !extConfig.Enabled {
            continue
        }

        ext := NewExtensionRuntime(extConfig)
        if err := ext.HealthCheck(); err != nil {
            ext.Status = "error"
        } else {
            ext.Status = "ready"
        }

        GlobalExtensionCache.Set(ext.ID, ext)
    }
    return nil
}
```

---

## 五、全局扩展缓存

应用启动后，所有成功解析的扩展都应放入全局缓存。

建议缓存结构：

```go
type ExtensionRuntime struct {
    ID          string
    Name        string
    Version     string
    Description string
    Enabled     bool
    Status      string
    Config      ExtensionConfig
    LoadedAt    time.Time
    LastError   string
}
```

状态建议：

| 状态 | 说明 |
|---|---|
| `loaded` | 已解析配置，但未完成健康检查 |
| `ready` | 可正常使用 |
| `disabled` | 配置禁用 |
| `error` | 初始化或健康检查失败 |

全局缓存能力建议：

```go
type ExtensionCache interface {
    Get(id string) (*ExtensionRuntime, bool)
    List() []*ExtensionRuntime
    Set(id string, ext *ExtensionRuntime)
    Delete(id string)
}
```

---

## 六、Studio 与扩展通信

主线程通过 Studio 与扩展通信。Studio 不直接扫描文件系统，而是通过主线程提供的扩展缓存获取扩展列表，并通过主线程转发消息。

通信链路：

```text
Studio 前端
  ↓ WebSocket / HTTP
主线程
  ↓ 查询全局扩展缓存
扩展 Runtime
  ↓ HTTP / 后续可扩展为进程 IPC、WebSocket、标准输入输出
具体扩展服务
```

### Studio 获取扩展列表

请求：

```http
GET /api/studio/extensions
```

响应：

```json
{
  "ok": true,
  "extensions": [
    {
      "id": "wechat",
      "name": "微信接入扩展",
      "version": "1.0.0",
      "status": "ready",
      "description": "提供微信登录、注册、消息推送与 Studio 通信能力",
      "studio": {
        "enabled": true,
        "channel": "wechat"
      },
      "capabilities": {
        "login": true,
        "register": true,
        "pushTypes": [
          "chat.message",
          "chat.event",
          "user.login",
          "user.register",
          "workspace.bind"
        ]
      }
    }
  ]
}
```

### Studio 向扩展发送消息

请求：

```http
POST /api/studio/extensions/{extensionId}/message
```

请求体：

```json
{
  "type": "studio.command",
  "requestId": "req_1770000000000_abcd",
  "timestamp": "2026-05-13T00:00:00Z",
  "payload": {
    "command": "reload-config"
  }
}
```

主线程收到后根据 `extensionId` 从全局缓存查找扩展，再转发给扩展配置中的 `entry.baseUrl`。

转发到扩展的默认路径建议为：

```http
POST {entry.baseUrl}/studio/message
```

扩展响应：

```json
{
  "ok": true,
  "requestId": "req_1770000000000_abcd",
  "type": "studio.command.result",
  "payload": {
    "message": "配置已重新加载"
  }
}
```

---

## 七、统一 JSON 通信格式

所有主线程、Studio、扩展之间的消息建议统一使用以下 envelope。

### 请求格式

```json
{
  "protocolVersion": "1.0",
  "type": "chat.message",
  "requestId": "req_1770000000000_abcd",
  "messageId": "msg_1770000000000_1234",
  "sessionId": "session_xxx",
  "workspaceId": "workspace_xxx",
  "instanceId": "instance_xxx",
  "timestamp": "2026-05-13T00:00:00Z",
  "source": "studio",
  "target": "wechat",
  "payload": {}
}
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `protocolVersion` | 字符串 | 是 | 协议版本，首版为 `1.0` |
| `type` | 字符串 | 是 | 消息类型 |
| `requestId` | 字符串 | 否 | 请求 ID，用于请求响应关联 |
| `messageId` | 字符串 | 否 | 消息 ID |
| `sessionId` | 字符串 | 否 | 会话 ID |
| `workspaceId` | 字符串 | 否 | 工作区 ID |
| `instanceId` | 字符串 | 否 | 实例 ID |
| `timestamp` | 字符串 | 是 | ISO 时间 |
| `source` | 字符串 | 是 | 来源，例如 `studio`、`server`、`extension` |
| `target` | 字符串 | 否 | 目标扩展 ID |
| `payload` | 对象 | 否 | 业务数据 |

### 响应格式

```json
{
  "protocolVersion": "1.0",
  "ok": true,
  "type": "chat.message.result",
  "requestId": "req_1770000000000_abcd",
  "timestamp": "2026-05-13T00:00:01Z",
  "source": "wechat",
  "payload": {
    "status": "accepted"
  }
}
```

失败响应：

```json
{
  "protocolVersion": "1.0",
  "ok": false,
  "type": "error",
  "requestId": "req_1770000000000_abcd",
  "timestamp": "2026-05-13T00:00:01Z",
  "source": "wechat",
  "error": {
    "code": "EXTENSION_BAD_REQUEST",
    "message": "不支持的消息类型"
  }
}
```

---

## 八、扩展登录注册 HTTP 接口

如果扩展配置中声明了登录或注册接口，主线程可以把相关请求转发给扩展。

配置示例：

```yaml
http:
  login:
    enabled: true
    path: /api/wechat/login
    method: POST
  register:
    enabled: true
    path: /api/wechat/register
    method: POST
```

### 登录接口

主线程对外提供统一入口：

```http
POST /api/extensions/{extensionId}/login
```

请求体：

```json
{
  "protocolVersion": "1.0",
  "type": "user.login",
  "requestId": "req_login_001",
  "timestamp": "2026-05-13T00:00:00Z",
  "source": "web",
  "payload": {
    "code": "wx_login_code",
    "redirectUri": "https://example.com/callback",
    "state": "random_state"
  }
}
```

主线程转发到：

```text
{entry.baseUrl}{http.login.path}
```

例如：

```http
POST http://127.0.0.1:19001/api/wechat/login
```

扩展响应：

```json
{
  "protocolVersion": "1.0",
  "ok": true,
  "type": "user.login.result",
  "requestId": "req_login_001",
  "timestamp": "2026-05-13T00:00:01Z",
  "source": "wechat",
  "payload": {
    "userId": "user_001",
    "username": "张三",
    "avatar": "https://example.com/avatar.png",
    "accessToken": "token_xxx",
    "expiresIn": 7200
  }
}
```

### 注册接口

主线程对外提供统一入口：

```http
POST /api/extensions/{extensionId}/register
```

请求体：

```json
{
  "protocolVersion": "1.0",
  "type": "user.register",
  "requestId": "req_register_001",
  "timestamp": "2026-05-13T00:00:00Z",
  "source": "web",
  "payload": {
    "code": "wx_register_code",
    "nickname": "张三",
    "avatar": "https://example.com/avatar.png"
  }
}
```

扩展响应：

```json
{
  "protocolVersion": "1.0",
  "ok": true,
  "type": "user.register.result",
  "requestId": "req_register_001",
  "timestamp": "2026-05-13T00:00:01Z",
  "source": "wechat",
  "payload": {
    "userId": "user_001",
    "username": "张三",
    "created": true
  }
}
```

如果扩展配置中没有声明登录接口，则主线程应返回：

```json
{
  "ok": false,
  "error": {
    "code": "EXTENSION_LOGIN_NOT_SUPPORTED",
    "message": "扩展未声明登录接口"
  }
}
```

如果扩展配置中没有声明注册接口，则主线程应返回：

```json
{
  "ok": false,
  "error": {
    "code": "EXTENSION_REGISTER_NOT_SUPPORTED",
    "message": "扩展未声明注册接口"
  }
}
```

---

## 九、HTTP 配置推送 type

扩展可以在配置中声明允许接收的 HTTP 推送 type。

配置示例：

```yaml
http:
  push:
    enabled: true
    path: /api/wechat/push
    method: POST
    types:
      - chat.message
      - chat.event
      - user.login
      - user.register
      - workspace.bind
```

主线程对外提供统一推送入口：

```http
POST /api/extensions/{extensionId}/push
```

请求体：

```json
{
  "protocolVersion": "1.0",
  "type": "chat.message",
  "requestId": "req_push_001",
  "messageId": "msg_001",
  "sessionId": "session_001",
  "workspaceId": "workspace_001",
  "instanceId": "instance_001",
  "timestamp": "2026-05-13T00:00:00Z",
  "source": "server",
  "target": "wechat",
  "payload": {
    "role": "assistant",
    "content": "你好"
  }
}
```

主线程处理规则：

1. 根据 `{extensionId}` 从全局缓存查找扩展。
2. 检查扩展是否启用。
3. 检查扩展是否配置了 `http.push.enabled = true`。
4. 检查请求中的 `type` 是否在 `http.push.types` 白名单中。
5. 通过后转发到：

```text
{entry.baseUrl}{http.push.path}
```

如果 type 不在白名单中，返回：

```json
{
  "ok": false,
  "error": {
    "code": "EXTENSION_PUSH_TYPE_NOT_ALLOWED",
    "message": "扩展未允许该推送类型"
  }
}
```

### 建议内置 type

| type | 说明 |
|---|---|
| `chat.message` | 聊天消息推送 |
| `chat.event` | 聊天事件推送 |
| `chat.delta` | 流式文本片段 |
| `chat.tool_call_started` | 工具调用开始 |
| `chat.tool_call_delta` | 工具调用参数增量 |
| `chat.tool_call_completed` | 工具调用完成 |
| `chat.tool_result` | 工具结果 |
| `user.login` | 用户登录 |
| `user.register` | 用户注册 |
| `workspace.bind` | 工作区绑定 |
| `workspace.unbind` | 工作区解绑 |
| `studio.command` | Studio 指令 |
| `studio.event` | Studio 事件 |

---

## 十、扩展健康检查

如果配置了：

```yaml
entry:
  healthPath: /health
```

主线程启动时应请求：

```http
GET {entry.baseUrl}{entry.healthPath}
```

建议响应：

```json
{
  "ok": true,
  "extensionId": "wechat",
  "status": "ready",
  "version": "1.0.0"
}
```

健康检查失败时：

- 扩展仍可写入缓存
- 状态标记为 `error`
- `lastError` 记录错误原因
- Studio 列表中可展示错误状态

---

## 十一、安全建议

扩展系统建议具备以下安全限制：

1. 扩展 ID 必须全局唯一。
2. 扩展只能访问配置声明的能力。
3. HTTP push 必须校验 type 白名单。
4. Studio 调用扩展前必须校验用户权限。
5. 扩展登录和注册接口必须走主线程统一入口。
6. 扩展响应必须限制大小，避免大响应占用内存。
7. 扩展错误不能直接暴露内部堆栈给前端。
8. 后续可为扩展增加签名字段，例如 `signature`。

---

## 十二、后续可扩展方向

后续可以继续扩展：

- 扩展热加载
- 扩展禁用/启用
- 扩展卸载
- 扩展权限审批
- 扩展市场
- 扩展日志面板
- 扩展进程托管
- 扩展 WebSocket 通道
- 扩展标准输入输出协议
- 扩展沙箱运行

---

## 十三、首期实现建议

首期建议只实现最小闭环：

1. 服务端配置增加扩展目录。
2. 启动时扫描 `extensions/*/extension.yaml`。
3. 解析扩展配置并放入全局缓存。
4. Studio 提供扩展列表接口。
5. Studio 提供扩展消息转发接口。
6. 支持扩展登录接口转发。
7. 支持扩展注册接口转发。
8. 支持扩展 HTTP push，并校验 type 白名单。
9. 所有通信统一使用 JSON envelope。

这样可以先让扩展机制跑通，再逐步增加热加载、权限、日志和沙箱能力。
