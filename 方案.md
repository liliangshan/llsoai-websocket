# Go WebSocket 与 HTTP 通信应用方案

## 一、目标

在本项目中规划一个 Go 应用，作为扩展端与用户端之间的通信中转服务。

整体角色分为三方：

- A：扩展端
  - 通过 WebSocket 连接到 B。
  - 连接地址形如：`ws://服务地址/ws?token=用户令牌`。
  - 连接成功后，上报工作区等扩展端信息。
- B：本 Go 应用
  - 提供 WebSocket 服务给 A 连接。
  - 提供 HTTP 接口与 SSE 流给 C 使用。
  - 维护全局在线连接缓存。
  - 根据 C 请求中的工作区标志，路由请求到对应 A。
  - 接收 A 返回结果后，通过 HTTP 或 SSE 返回给 C。
- C：用户端
  - 通过 POST 请求调用 B。
  - 可通过 SSE 接收流式响应。
  - 请求时携带工作区标志，用于指定目标扩展端。

## 二、数据库参考配置

数据库配置参考文件：

`/Users/lls/liliangshan/openaisdk/cmd/api/config.yaml`

参考配置如下：

```yaml
database:
  host: "127.0.0.1"
  port: 3306
  username: "openai"
  password: "${DB_PASSWORD}"
  name: "openai"
  charset: "utf8mb4"
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: 5m
```

说明：方案文档中不保留真实数据库密码，正式配置建议通过环境变量或密钥管理系统注入。

当前 MCP 查看结果：

- 数据库：`openai`
- 用户表：`users`
- 当前数据库允许 DDL、DROP、DELETE 等操作

当前 `users` 表主要字段：

- `id`
- `username`
- `password`
- `email`
- `plan_id`
- `plan_expire_at`
- `is_admin`
- `created_at`
- `updated_at`

## 三、数据库变更方案

需要在 `users` 表中增加用于 WebSocket 连接认证的 token 哈希字段。明文 token 只在生成时展示一次，不入库、不进日志。

建议字段：

```sql
ALTER TABLE users
ADD COLUMN websocket_token_hash varchar(128) DEFAULT NULL COMMENT 'WebSocket 连接令牌哈希',
ADD COLUMN websocket_token_prefix varchar(12) DEFAULT NULL COMMENT 'WebSocket 连接令牌前缀，用于辅助定位和脱敏展示',
ADD COLUMN websocket_token_last_used_at datetime DEFAULT NULL COMMENT 'WebSocket 令牌最近使用时间',
ADD COLUMN websocket_token_rotated_at datetime DEFAULT NULL COMMENT 'WebSocket 令牌最近轮换时间',
ADD COLUMN websocket_token_disabled_at datetime DEFAULT NULL COMMENT 'WebSocket 令牌禁用时间';
```

为了保证一个用户只能有一个 WebSocket token，token 哈希字段直接存储在 `users` 表中即可。

为了保证 token 全局唯一，建议增加唯一索引：

```sql
ALTER TABLE users
ADD UNIQUE KEY uk_users_websocket_token_hash (websocket_token_hash);
```

注意：MySQL 唯一索引允许多个 NULL，因此未设置 token 的用户不会冲突。

需要新增用户时，可使用现有 `users` 表结构创建一条用户记录，并填入唯一的 `websocket_token_hash`。

token 生成规则：

1. 生成至少三十二字节随机值。
2. 使用安全编码生成明文 token。
3. 对明文 token 计算哈希后写入 `websocket_token_hash`。
4. 将 token 前缀写入 `websocket_token_prefix`，仅用于辅助定位和脱敏展示。
5. 明文 token 只返回给用户一次，服务端不保存。
6. 用户密码哈希算法以现有 `openai` 库的密码哈希实现为准。

示例：

```sql
INSERT INTO users (
  username,
  password,
  email,
  websocket_token_hash,
  websocket_token_prefix,
  websocket_token_rotated_at,
  is_admin
) VALUES (
  'websocket_user',
  '待按项目密码规则生成的密码哈希',
  'websocket_user@example.com',
  '待写入的唯一令牌哈希',
  '令牌前缀',
  NOW(),
  0
);
```

本阶段只做方案，不实际执行建表、改表、插入用户。

## 四、配置文件方案

建议在本项目中新增配置文件，例如：

`config.yaml`

建议内容：

```yaml
app:
  host: "0.0.0.0"
  port: 28081

database:
  host: "127.0.0.1"
  port: 3306
  username: "openai"
  password: "${DB_PASSWORD}"
  name: "openai"
  charset: "utf8mb4"
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: 5m

websocket:
  path: "/ws"
  read_timeout: 60s
  write_timeout: 60s
  heartbeat_interval: 30s

http:
  request_timeout: 120s
  sse_ping_interval: 15s
```

## 五、全局连接中心设计

B 需要维护 A 的在线连接缓存。正式方案不只使用单一工作区索引，而是设计连接中心，支持按用户、工作区、实例和请求多维路由。

概念结构：

```go
type Hub struct {
  ByWorkspace map[string]map[string]*WorkspaceClient
  ByInstance  map[string]*WorkspaceClient
  ByUser      map[uint64]map[string]*WorkspaceClient
  Pending     map[string]*PendingRequest
}

type WorkspaceClient struct {
  UserID       uint64
  WorkspaceID  string
  InstanceID   string
  SessionID    string
  Conn         *websocket.Conn
  Send         chan Envelope
  Metadata     map[string]any
  ConnectedAt  time.Time
  LastSeenAt   time.Time
  State        string
}
```

索引说明：

- `ByWorkspace`：按工作区标识索引，同一工作区下可有多个扩展实例。
- `ByInstance`：按扩展实例标识索引，用于精确路由。
- `ByUser`：按用户标识索引，用于权限校验和连接数限制。
- `Pending`：按请求标识索引等待中的普通请求或流式请求。

并发要求：

- 连接中心需要读写锁保护。
- `Pending` 可独立加锁或使用并发安全结构，避免高频流式事件与连接索引争用同一把锁。
- WebSocket 不能并发写，同一连接所有出站消息必须写入 `Send` 队列，由独立写协程串行发送。
- `Send` 队列建议容量为二百五十六条。
- WebSocket 单次写超时建议十秒。
- `Send` 队列写满时，视为扩展端消费异常，应关闭该连接，并将相关 pending 标记为 `upstream_write_failed`。
- 写协程通过关闭 `Send` 队列或接收连接关闭信号退出，关闭过程必须保证只执行一次。
- 断开连接时必须从全部索引中移除，并关闭关联发送队列。
- 断开连接时必须把该连接关联的未完成请求标记为 `client_disconnected`。
- 空工作区、空用户索引需要及时删除，避免内存泄漏。

同一用户一个 token 表示一个用户只有一个认证令牌，不表示只能建立一个连接。正式方案允许同一用户打开多个工作区或多个窗口，每个连接通过唯一 `instanceId` 区分。若后续需要单连接模式，可在新连接建立后踢掉旧连接。

## 六、A 到 B 的 WebSocket 流程

### 连接认证

A 通过以下地址连接：

```text
ws://服务地址/ws?token=用户令牌
```

B 处理流程：

1. 从 query 中读取 `token`。
2. 对 token 计算哈希。
3. 查询 `users.websocket_token_hash` 并校验令牌未被禁用。
4. token 不存在、无效或已禁用，拒绝连接。
5. token 有效，升级为 WebSocket 连接。
6. 等待 A 上报工作区信息。

### A 上报类型补充

参考扩展端文档：

`/Users/lls/liliangshan/vcode/liliangshan.openapi-compatible-copilot/REMOTE_NOTIFICATION_MANAGEMENT_PLAN.md`

扩展端连接 B 后，不只是上报工作区信息，还会按照远程通知协议持续上报模型流式事件、工具事件、完成事件、取消事件、错误事件、测试通知和统计信息。

扩展端使用统一消息信封，B 按以下字段实现：

```json
{
  "protocolVersion": "1.0",
  "type": "message.type",
  "messageId": "msg_当前消息唯一标识",
  "eventId": "evt_事件唯一标识",
  "eventSeq": 1,
  "sessionId": "当前会话标识",
  "requestId": "当前请求标识",
  "workspaceId": "工作区标识",
  "instanceId": "扩展实例标识",
  "timestamp": "2026-05-12T00:00:00.000Z",
  "source": "vscode-extension",
  "payload": {}
}
```

字段说明：

- `protocolVersion`：协议版本。
- `type`：消息类型。
- `messageId`：单条消息唯一标识，用于去重。
- `eventId`：业务事件唯一标识，用于去重。
- `eventSeq`：同一会话和请求内单调递增的事件序号。
- `sessionId`：聊天会话标识。
- `requestId`：一次模型请求标识。
- `workspaceId`：工作区标识，是 B 全局缓存和路由的核心字段。
- `instanceId`：扩展实例标识，用于区分多窗口和多实例。
- `timestamp`：事件产生时间。
- `source`：消息来源，扩展端固定为 `vscode-extension`。
- `payload`：具体业务内容。

扩展端发送给 B 的类型如下：

| 类型 | 用途 | B 的处理建议 |
| --- | --- | --- |
| `client.hello` | 扩展连接后发送握手信息 | 校验协议版本、记录扩展能力，返回 `server.hello_ack` |
| `client.connection_context` | 连接成功后发送会话标识和工作区目录 | 提取 `workspaceId`、`instanceId` 和工作区目录，写入连接中心多索引 |
| `client.heartbeat_ping` | 扩展发送心跳请求 | 返回 `server.heartbeat_pong` |
| `client.heartbeat_pong` | 扩展回复远端心跳 | 更新连接最后活跃时间 |
| `model.request_started` | 模型请求开始 | 可转发给 C 或记录请求开始状态 |
| `model.text_delta` | 模型正文流式片段 | 写入对应 SSE 流或缓存 |
| `model.reasoning_delta` | 模型推理流式片段 | 按配置决定是否转发给 C |
| `model.tool_call_started` | 工具调用开始 | 写入对应 SSE 流或事件缓存 |
| `model.tool_call_delta` | 工具调用参数增量 | 写入对应 SSE 流或事件缓存 |
| `model.tool_call_completed` | 工具调用参数完成 | 写入对应 SSE 流或事件缓存 |
| `model.tool_result` | 工具结果 | 写入对应 SSE 流或事件缓存 |
| `model.assistant_final` | 最终助手消息 | 普通 POST 可用该事件作为最终响应来源 |
| `model.request_completed` | 模型请求完成 | 结束对应请求或 SSE 流 |
| `model.request_cancelled` | 模型请求取消 | 返回取消错误并清理 pending |
| `model.request_error` | 模型请求错误 | 返回错误并清理 pending |
| `notify.test` | 测试通知 | 用于连通性测试和调试 |
| `notify.metrics` | 远程通知统计信息 | 可记录统计，不影响主流程 |
| `client.chat_history_response` | 扩展返回当前项目历史聊天记录 | 匹配历史请求 pending，并返回给 C |
| `client.chat_history_error` | 扩展返回历史聊天记录读取失败原因 | 匹配历史请求 pending，并返回错误给 C |

B 发送给扩展端的类型如下：

| 类型 | 用途 |
| --- | --- |
| `server.hello_ack` | B 对扩展握手的确认 |
| `server.heartbeat_ping` | B 主动发送心跳请求，首版暂不使用 |
| `server.heartbeat_pong` | B 回复扩展心跳 |
| `server.ack` | B 确认收到扩展消息 |
| `server.error` | B 返回错误 |
| `server.chat_message` | B 要求扩展写入聊天输入 |
| `server.chat_history_request` | B 请求扩展端读取当前项目历史聊天记录 |

### 握手与连接上下文上报

扩展端连接成功后，首先发送 `client.hello`。

示例：

```json
{
  "protocolVersion": "1.0",
  "type": "client.hello",
  "messageId": "msg_hello_唯一标识",
  "eventId": "evt_hello_唯一标识",
  "eventSeq": 0,
  "sessionId": "当前会话标识",
  "requestId": "",
  "workspaceId": "工作区标识",
  "instanceId": "扩展实例标识",
  "timestamp": "2026-05-12T00:00:00.000Z",
  "source": "vscode-extension",
  "payload": {
    "extensionName": "LLS OAI",
    "extensionVersion": "扩展版本",
    "language": "界面语言",
    "capabilities": {
      "streamEvents": true,
      "toolEvents": true,
      "inboundChatMessage": true,
      "webhook": true,
      "heartbeat": true
    }
  }
}
```

B 应返回 `server.hello_ack`：

```json
{
  "protocolVersion": "1.0",
  "type": "server.hello_ack",
  "messageId": "msg_ack_唯一标识",
  "eventId": "evt_ack_唯一标识",
  "eventSeq": 0,
  "sessionId": "当前会话标识",
  "requestId": "",
  "workspaceId": "工作区标识",
  "instanceId": "go-server",
  "timestamp": "2026-05-12T00:00:00.000Z",
  "source": "remote-server",
  "payload": {
    "accepted": true,
    "serverName": "llsoai-websocket",
    "serverVersion": "服务版本",
    "heartbeatIntervalMs": 30000,
    "enabledCapabilities": {
      "streamEvents": true,
      "toolEvents": true,
      "inboundChatMessage": true
    },
    "errorCode": "",
    "errorMessage": ""
  }
}
```

握手确认后，扩展端发送 `client.connection_context`，该消息是 B 建立工作区路由缓存的主要依据。

示例：

```json
{
  "protocolVersion": "1.0",
  "type": "client.connection_context",
  "messageId": "msg_context_唯一标识",
  "eventId": "evt_context_唯一标识",
  "eventSeq": 0,
  "sessionId": "当前会话标识",
  "requestId": "",
  "workspaceId": "工作区标识",
  "instanceId": "扩展实例标识",
  "timestamp": "2026-05-12T00:00:00.000Z",
  "source": "vscode-extension",
  "payload": {
    "workspaceFolders": [
      {
        "name": "工作区名称",
        "path": "脱敏后的工作区目录",
        "isPrimary": true
      }
    ],
    "activeWorkspaceFolder": "脱敏后的当前主工作区目录"
  }
}
```

B 收到 `client.connection_context` 后：

1. 校验 `workspaceId` 不能为空。
2. 记录 `sessionId`、`instanceId`、`workspaceId`。
3. 将工作区目录列表保存到连接元数据。
4. 把连接加入 `Hub.ByWorkspace[workspaceId][instanceId]`。
5. 同步写入 `Hub.ByInstance[instanceId]` 和 `Hub.ByUser[userId][instanceId]`。
6. 返回 `server.ack`。

工作区路径已经由扩展端脱敏，规则为只保留最后两个目录，不足两个目录时只保留最后一个目录。B 不应要求扩展端上传完整绝对路径。

### 确认、错误与心跳规则

- B 对 `client.hello` 和 `client.connection_context` 必须返回确认。
- B 收到不支持的协议版本、缺少 `workspaceId`、缺少 `instanceId`、消息体过大或消息格式错误时，应返回 `server.error` 并按错误严重程度决定是否断开连接。
- B 返回给 A 的消息也需要维护连接维度的递增 `eventSeq`。
- 首版心跳由扩展端主动发起，B 响应 `server.heartbeat_pong`。
- B 不主动发送 `server.heartbeat_ping`，但保留该类型用于后续扩展。
- B 通过 `LastSeenAt` 判断连接是否超时，超过配置阈值后断开连接并触发断开清理。

### 断开清理

当 A 断开连接、心跳超时或读写错误时：

1. 关闭 WebSocket 连接。
2. 从 `Hub.ByWorkspace`、`Hub.ByInstance`、`Hub.ByUser` 三个索引中移除对应连接。
3. 将该连接相关 pending 标记为 `client_disconnected`。
4. 关闭发送队列，停止心跳任务和写协程。
5. 如果 `Hub.ByWorkspace[workspaceId]`、`Hub.ByUser[userId]` 索引为空，删除对应 key，避免内存泄漏。

## 七、C 到 B 到 A 的正式链路

正式链路统一使用扩展端远程通知协议。C 请求 B 后，B 向扩展端发送 `server.chat_message`。扩展端收到后写入聊天输入或自动发送，并把模型过程事件以 `model.*` 类型上报给 B。B 根据 `requestId` 关联等待请求，并把结果返回给 C。

主链路：

```text
C --POST/SSE--> B --server.chat_message--> A
A --model.*--> B --JSON/SSE--> C
```

### 普通 POST 接口

建议接口：

```text
POST /api/chat
```

C 请求 B：

```json
{
  "workspaceId": "目标工作区标识",
  "instanceId": "可选，目标扩展实例标识",
  "sessionId": "可选，目标会话标识",
  "text": "需要发送给扩展端聊天输入的内容",
  "autoSend": true,
  "dedupeKey": "可选去重键"
}
```

B 处理流程：

1. 校验 C 的 HTTP 鉴权。
2. 校验 C 是否有权访问目标用户、工作区和实例。
3. 读取 `workspaceId`、可选 `instanceId`、可选 `sessionId`。
4. 生成 `requestId`、`messageId`、`eventId`、时间戳和去重键。
5. 从连接中心路由到目标扩展端连接。
6. 创建 `PendingRequest`。
7. 通过 WebSocket 向 A 发送 `server.chat_message`。
8. 等待 A 返回同一 `requestId` 的 `model.assistant_final` 和 `model.request_completed`。
9. 将最终结果作为 JSON 返回给 C。

B 发给 A 的消息：

```json
{
  "protocolVersion": "1.0",
  "type": "server.chat_message",
  "messageId": "msg_唯一标识",
  "eventId": "evt_唯一标识",
  "eventSeq": 0,
  "sessionId": "目标会话标识",
  "requestId": "req_唯一请求标识",
  "workspaceId": "工作区标识",
  "instanceId": "go-server",
  "timestamp": "2026-05-12T00:00:00.000Z",
  "source": "remote-server",
  "payload": {
    "text": "需要写入聊天输入的内容",
    "autoSend": true,
    "bypassPromptEnhancement": true,
    "dedupeKey": "去重键",
    "expireAt": "2026-05-12T00:05:00.000Z"
  }
}
```

B 返回给 C：

```json
{
  "requestId": "req_唯一请求标识",
  "ok": true,
  "workspaceId": "工作区标识",
  "instanceId": "扩展实例标识",
  "payload": {
    "text": "最终助手可见正文",
    "finishReason": "stop"
  },
  "error": null
}
```

普通 POST 建议以 `model.assistant_final` 作为最终文本来源，以 `model.request_completed` 作为请求完成确认。如果只收到完成事件但没有最终文本，应返回空文本或业务错误，具体由后续实现策略确定。

### SSE 流式接口

建议接口：

```text
POST /api/chat/stream
```

说明：

- C 使用 POST 发起流式请求。
- B 返回 `text/event-stream`。
- B 先向 A 发送 `server.chat_message`。
- A 上报的 `model.*` 事件由 B 映射为 SSE 事件返回给 C。

SSE 映射建议：

| WebSocket 类型 | SSE event | 说明 |
| --- | --- | --- |
| `model.request_started` | `model.request_started` | 模型请求开始 |
| `model.text_delta` | `model.text_delta` | 正文增量 |
| `model.reasoning_delta` | `model.reasoning_delta` | 推理增量，可按配置关闭 |
| `model.tool_call_started` | `model.tool_call_started` | 工具调用开始 |
| `model.tool_call_delta` | `model.tool_call_delta` | 工具参数增量 |
| `model.tool_call_completed` | `model.tool_call_completed` | 工具调用完成 |
| `model.tool_result` | `model.tool_result` | 工具结果 |
| `model.assistant_final` | `model.assistant_final` | 最终助手消息 |
| `model.request_completed` | `done` | 流式结束 |
| `model.request_cancelled` | `cancelled` | 请求取消 |
| `model.request_error` | `error` | 请求错误 |

SSE 正文片段示例：

```text
event: model.text_delta
data: {"requestId":"req_xxx","delta":"内容片段"}

```

SSE 结束示例：

```text
event: done
data: {"requestId":"req_xxx"}

```

SSE 错误示例：

```text
event: error
data: {"requestId":"req_xxx","code":"request_timeout","message":"请求超时"}

```

SSE 取消示例：

```text
event: cancelled
data: {"requestId":"req_xxx","code":"request_cancelled","message":"请求已取消"}

```

## 八、请求关联设计

B 需要维护等待响应的请求表，按 `requestId` 关联 C 请求、B 发给 A 的 `server.chat_message`、A 返回的 `model.*` 事件。

建议结构：

```go
type PendingRequest struct {
  RequestID   string
  UserID      uint64
  WorkspaceID string
  InstanceID  string
  SessionID   string
  Mode        string
  State       string
  Response    chan Envelope
  Stream      chan Envelope
  Final       *Envelope
  CreatedAt   time.Time
  ExpiresAt   time.Time
}
```

请求状态：

- `created`：已创建等待请求。
- `sent_to_extension`：已发送到扩展端。
- `started`：收到 `model.request_started`。
- `streaming`：正在接收流式事件。
- `completed`：收到 `model.request_completed`。
- `cancelled`：收到 `model.request_cancelled`。
- `error`：收到 `model.request_error` 或内部错误。
- `timeout`：请求超时。
- `client_disconnected`：目标扩展端断开。
- `sse_disconnected`：C 的 SSE 连接断开。
- `history_responded`：历史记录请求已收到 `client.chat_history_response`。
- `history_failed`：历史记录请求已收到 `client.chat_history_error` 或本地处理失败。

处理规则：

- HTTP 普通请求使用 `Response` 等待最终响应。
- SSE 请求使用 `Stream` 接收多个 `Envelope`。
- 历史记录请求使用 `Mode=history`，等待 `client.chat_history_response` 或 `client.chat_history_error`。
- 历史记录请求不经过 `model.*` 系列状态，通常从 `created` 到 `sent_to_extension`，再结束为 `history_responded` 或 `history_failed`。
- A 返回 `model.text_delta` 等流式事件时，按 `requestId` 找到 pending 并写入流通道。
- A 返回 `model.assistant_final` 时，缓存最终文本。
- A 返回 `model.request_completed` 时，结束 pending。
- A 返回 `model.request_cancelled` 或 `model.request_error` 时，写入错误并结束 pending。
- 请求超时后清理 pending，避免内存泄漏。
- C 主动断开 SSE 时，首版只在 B 本地清理 pending，不主动通知扩展端取消。后续如需远端取消，需要在扩展端协议中新增反向取消类型。

## 九、历史聊天记录请求方案

扩展端新增支持 B 通过 WebSocket 主动请求当前项目历史聊天记录。B 对 C 暴露 HTTP 接口，C 请求 B 后，B 路由到目标扩展端，再通过 `server.chat_history_request` 请求 A。A 读取当前项目工作区目录下的项目聊天日志后，通过 `client.chat_history_response` 或 `client.chat_history_error` 返回给 B，B 再返回给 C。

主链路：

```text
C --POST /api/chat/history--> B --server.chat_history_request--> A
A --client.chat_history_response 或 client.chat_history_error--> B --JSON--> C
```

### C 请求 B 的接口

建议接口：

```text
POST /api/chat/history
```

C 请求 B：

```json
{
  "workspaceId": "目标工作区标识",
  "instanceId": "可选，目标扩展实例标识",
  "sessionId": "可选，目标会话标识",
  "limit": 100,
  "order": "asc",
  "scope": "project"
}
```

字段规则：

- `workspaceId` 必填，用于路由到目标扩展端。
- `instanceId` 可选，存在时精确路由。
- `sessionId` 可选，首版只用于回显和链路追踪，不参与历史记录筛选。
- `limit` 表示期望返回的最大消息数量，首版最大为一百。
- `order` 首版固定为 `asc`，表示时间正序。非 `asc` 时返回 `invalid_chat_history_request`。
- `scope` 首版固定为 `project`，只允许请求当前项目历史记录，不允许请求全局历史记录。

完整 HTTP 接口包括普通聊天接口 `POST /api/chat`、流式聊天接口 `POST /api/chat/stream` 和历史记录接口 `POST /api/chat/history`。

B 处理流程：

1. 校验 C 的 HTTP 鉴权。
2. 校验 C 是否有权访问目标用户、工作区和实例。
3. 校验 `scope` 必须为 `project`。
4. 校验 `order` 必须为 `asc`。
5. 校验 `limit` 范围，超过一百时按一百处理，小于一时返回 `invalid_chat_history_request`。
6. 根据 `workspaceId` 和可选 `instanceId` 路由到 `ready` 状态的扩展端连接。
7. 生成历史请求专用 `requestId`、`messageId`、`eventId` 和时间戳。
8. 创建模式为 `history` 的 `PendingRequest`。
9. 发送 `server.chat_history_request` 给扩展端。
10. 等待 `client.chat_history_response` 或 `client.chat_history_error`。
11. 将扩展端响应转换为 JSON 返回给 C。

B 发给 A 的消息：

```json
{
  "protocolVersion": "1.0",
  "type": "server.chat_history_request",
  "messageId": "msg_history_request_唯一标识",
  "eventId": "evt_history_request_唯一标识",
  "eventSeq": 0,
  "sessionId": "目标会话标识或空字符串",
  "requestId": "history_request_唯一标识",
  "workspaceId": "工作区标识",
  "instanceId": "go-server",
  "timestamp": "2026-05-12T00:00:00.000Z",
  "source": "remote-server",
  "payload": {
    "limit": 100,
    "order": "asc",
    "scope": "project"
  }
}
```

A 成功返回给 B：

```json
{
  "protocolVersion": "1.0",
  "type": "client.chat_history_response",
  "messageId": "msg_history_response_唯一标识",
  "eventId": "evt_history_response_唯一标识",
  "eventSeq": 0,
  "sessionId": "目标会话标识或空字符串",
  "requestId": "history_request_唯一标识",
  "workspaceId": "工作区标识",
  "instanceId": "扩展实例标识",
  "timestamp": "2026-05-12T00:00:00.000Z",
  "source": "vscode-extension",
  "payload": {
    "enabled": true,
    "scope": "project",
    "workspaceFolder": "脱敏后的当前主工作区目录",
    "messageCount": 100,
    "limit": 100,
    "truncated": true,
    "truncatedReason": "message_limit",
    "messages": [
      {
        "role": "user",
        "content": "历史消息内容",
        "timestamp": "2026-05-12T00:00:00.000Z"
      }
    ]
  }
}
```

A 失败返回给 B：

```json
{
  "protocolVersion": "1.0",
  "type": "client.chat_history_error",
  "messageId": "msg_history_error_唯一标识",
  "eventId": "evt_history_error_唯一标识",
  "eventSeq": 0,
  "sessionId": "目标会话标识或空字符串",
  "requestId": "history_request_唯一标识",
  "workspaceId": "工作区标识",
  "instanceId": "扩展实例标识",
  "timestamp": "2026-05-12T00:00:00.000Z",
  "source": "vscode-extension",
  "payload": {
    "enabled": false,
    "scope": "project",
    "errorCode": "PROJECT_CHAT_HISTORY_DISABLED",
    "errorMessage": "当前项目未开启日志保存",
    "retryable": false
  }
}
```

B 返回给 C 的成功响应：

```json
{
  "requestId": "history_request_唯一标识",
  "ok": true,
  "workspaceId": "工作区标识",
  "instanceId": "扩展实例标识",
  "payload": {
    "enabled": true,
    "scope": "project",
    "workspaceFolder": "脱敏后的当前主工作区目录",
    "messageCount": 100,
    "limit": 100,
    "truncated": true,
    "truncatedReason": "message_limit",
    "messages": []
  },
  "error": null
}
```

B 返回给 C 的失败响应：

```json
{
  "requestId": "history_request_唯一标识",
  "ok": false,
  "workspaceId": "工作区标识",
  "instanceId": "扩展实例标识",
  "payload": null,
  "error": {
    "code": "PROJECT_CHAT_HISTORY_DISABLED",
    "message": "当前项目未开启日志保存",
    "retryable": false
  }
}
```

### 历史记录请求约束

- B 不直接读取扩展端本地文件，只通过 WebSocket 请求扩展端读取。
- B 不允许 C 请求全局聊天记录，`scope` 必须为 `project`。
- B 不允许 C 请求超过一百条历史消息。
- `server.chat_history_request` 的 `instanceId` 表示 B 自身服务实例标识，例如 `go-server`。目标扩展实例通过 C 请求中的 `instanceId` 参与路由，不写入该出站消息的 `instanceId` 字段。
- `server.chat_history_request` 是单次请求，`eventSeq` 固定为零，不参与流式事件递增校验。
- `sessionId` 只用于回显和链路追踪，不参与扩展端历史记录文件筛选。
- B 不应在日志中记录历史消息正文。
- B 应限制历史记录响应最大体积，建议总响应最大二兆字节，单条消息最大三十二千字节。超过限制时返回错误或要求扩展端截断。
- `truncated=false` 时，`truncatedReason` 应省略或为 `null`。
- B 应对历史请求单独限流，建议单用户每分钟最多十次，单工作区每分钟最多五次。
- B 应对历史请求建立独立 pending 超时，建议三十秒。
- 历史记录 pending 与模型请求 pending 分桶限流，历史请求不能挤占模型请求并发额度。
- B 收到 `client.chat_history_response` 时，按 `requestId` 匹配 pending。
- B 收到 `client.chat_history_error` 时，按 `requestId` 匹配 pending，并转换为 C 可读错误。
- 如果 pending 不存在，B 应丢弃该响应并记录脱敏日志。

### 历史记录错误码

扩展端可能返回以下错误码，B 应原样保留并转发给 C：

| 错误码 | 含义 |
| --- | --- |
| `PROJECT_CHAT_HISTORY_DISABLED` | 当前项目未开启日志保存 |
| `NO_WORKSPACE` | 当前没有打开工作区 |
| `PROJECT_HISTORY_DIR_NOT_FOUND` | 当前项目日志目录不存在 |
| `PROJECT_HISTORY_READ_FAILED` | 项目日志读取失败 |
| `CHAT_HISTORY_REQUEST_DISABLED` | 远端历史请求功能未开启 |
| `CHAT_HISTORY_RATE_LIMITED` | 历史请求频率超限 |
| `CHAT_HISTORY_FILE_PARSE_FAILED` | 单个文件解析失败 |
| `CHAT_HISTORY_RESPONSE_TOO_LARGE` | 响应体超过大小限制 |
| `INVALID_CHAT_HISTORY_REQUEST` | 请求参数非法 |

B 自产历史记录错误码使用小写蛇形：

| 错误码 | 含义 |
| --- | --- |
| `invalid_chat_history_request` | C 请求参数非法，例如 `limit` 小于一、`scope` 不是 `project` 或 `order` 不是 `asc` |
| `history_request_timeout` | B 等待扩展端历史记录响应超时 |

错误码规则：

- A 返回的错误码保持大写蛇形，B 原样转发给 C。
- B 自己产生的错误码使用小写蛇形。
- `INVALID_CHAT_HISTORY_REQUEST` 表示扩展端判断请求非法。
- `invalid_chat_history_request` 表示 B 在转发给扩展端之前判断 C 请求非法。

### 历史记录 HTTP 状态码

建议 HTTP 状态码：

| 场景 | HTTP 状态码 | 响应体 |
| --- | --- | --- |
| 成功 | 200 | `ok=true` |
| C 请求参数错误 | 400 | `ok=false`，错误码 `invalid_chat_history_request` |
| C 未认证 | 401 | `ok=false`，错误码 `unauthorized` |
| C 无权限访问目标工作区或实例 | 403 | `ok=false`，错误码 `forbidden` |
| 历史请求被限流 | 429 | `ok=false`，错误码 `rate_limited` |
| 工作区或实例离线 | 503 | `ok=false`，错误码 `workspace_offline` 或 `instance_offline` |
| 等待扩展端响应超时 | 504 | `ok=false`，错误码 `history_request_timeout` |
| 扩展端业务错误 | 200 | `ok=false`，错误码原样透传扩展端错误码 |
| B 内部错误 | 500 | `ok=false`，错误码 `internal_error` |

## 十、消息协议设计

本项目的 WebSocket 协议使用扩展端远程通知文档中的统一信封协议，即字段风格使用 `protocolVersion`、`type`、`messageId`、`eventId`、`eventSeq`、`sessionId`、`requestId`、`workspaceId`、`instanceId`、`timestamp`、`source`、`payload`。

首版协议版本固定为 `1.0`。B 收到不同主版本号时，应返回 `unsupported_protocol_version` 并拒绝继续处理。

`timestamp` 统一使用 UTC 毫秒格式，例如 `2026-05-12T00:00:00.000Z`。

推荐统一消息结构：

```go
type Envelope struct {
    ProtocolVersion string         `json:"protocolVersion"`
    Type            string         `json:"type"`
    MessageID       string         `json:"messageId"`
    EventID         string         `json:"eventId"`
    EventSeq        int64          `json:"eventSeq"`
    SessionID       string         `json:"sessionId"`
    RequestID       string         `json:"requestId"`
    WorkspaceID     string         `json:"workspaceId"`
    InstanceID      string         `json:"instanceId"`
    Timestamp       string         `json:"timestamp"`
    Source          string         `json:"source"`
    Payload         map[string]any `json:"payload,omitempty"`
}
```

扩展端到 B 的核心事件中，`model.text_delta`、`model.reasoning_delta`、`model.tool_call_started`、`model.tool_call_delta`、`model.tool_call_completed`、`model.tool_result` 可作为 SSE 的事件来源；`model.assistant_final` 和 `model.request_completed` 可作为普通 POST 的最终响应依据；`model.request_cancelled` 和 `model.request_error` 应触发错误响应并清理等待请求。

B 到扩展端的用户输入消息使用 `server.chat_message`，用于 C 请求 B 后，B 请求扩展端写入聊天输入或自动发送。

示例：

```json
{
  "protocolVersion": "1.0",
  "type": "server.chat_message",
  "messageId": "msg_chat_唯一标识",
  "eventId": "evt_chat_唯一标识",
  "eventSeq": 0,
  "sessionId": "目标会话标识",
  "requestId": "当前请求标识",
  "workspaceId": "工作区标识",
  "instanceId": "go-server",
  "timestamp": "2026-05-12T00:00:00.000Z",
  "source": "remote-server",
  "payload": {
    "text": "需要写入聊天输入的内容",
    "autoSend": false,
    "bypassPromptEnhancement": true,
    "dedupeKey": "去重键",
    "expireAt": "2026-05-12T00:05:00.000Z"
  }
}
```

扩展端文档要求，远端入站消息必须跳过提示词优化。因此 B 发送 `server.chat_message` 时，`payload.bypassPromptEnhancement` 应固定为 `true`。

## 十一、错误处理方案

统一错误响应结构：

```json
{
  "code": "workspace_offline",
  "message": "目标工作区不在线",
  "requestId": "req_xxx",
  "details": {}
}
```

需要处理以下异常：

- token 缺失。
- token 无效。
- token 过期或被禁用。
- HTTP 用户未认证。
- HTTP 用户无权访问目标工作区或实例。
- 工作区未上报。
- 工作区不存在或当前离线。
- 实例不存在或当前离线。
- 同工作区存在多个连接且无法唯一选择。
- A 响应超时。
- A 连接中途断开。
- C 客户端取消请求。
- SSE 写入失败。
- 消息体过大。
- 请求被限流。
- 消息重复。
- pending 请求未清理导致泄漏。

建议错误码：

- `invalid_protocol`
- `unsupported_protocol_version`
- `unauthorized`
- `forbidden`
- `token_expired`
- `workspace_required`
- `workspace_offline`
- `instance_offline`
- `ambiguous_workspace`
- `request_timeout`
- `message_too_large`
- `rate_limited`
- `duplicate_request`
- `client_disconnected`
- `sse_disconnected`
- `upstream_write_failed`
- `history_request_timeout`
- `invalid_chat_history_request`
- `internal_error`

## 十二、路由策略

路由优先级：

1. C 指定 `instanceId` 时，使用 `instanceId` 精确路由。
2. C 只指定 `workspaceId` 时，在该工作区下选择最新可用连接。
3. 如果工作区下存在多个连接且策略无法判断，返回 `ambiguous_workspace`，提示 C 指定 `instanceId`。
4. 如果目标连接不存在或不处于可用状态，返回 `workspace_offline` 或 `instance_offline`。

连接状态：

- `connecting`：连接建立中。
- `authenticated`：token 已认证。
- `hello_received`：已收到 `client.hello`。
- `context_received`：已收到 `client.connection_context`。
- `ready`：可被 C 请求路由。
- `closing`：关闭中。
- `closed`：已关闭。

只有 `ready` 状态的连接可以承载 C 的请求。

断开清理要求：

- 从 `ByWorkspace` 删除连接。
- 从 `ByInstance` 删除连接。
- 从 `ByUser` 删除连接。
- 关闭连接发送队列。
- 标记该连接相关 pending 为 `client_disconnected`。
- 释放 SSE 通道。
- 停止心跳任务和写协程。
- 删除空索引。

## 十三、安全、鉴权与治理方案

### WebSocket 鉴权

- A 通过 `ws://服务地址/ws?token=用户令牌` 连接 B。
- 生产环境建议使用 `wss://`。
- B 从地址中读取 token，计算哈希后查询用户表。
- token 无效、过期或被禁用时拒绝连接。
- token 不得进入日志、错误响应、状态页或指标标签。
- WebSocket 地址展示时必须脱敏。
- token 日志脱敏样式建议为 `abcd***wxyz`，只显示前四位和后四位。
- 如果使用反向代理，代理访问日志也必须脱敏 query string 中的 token。

### token 存储建议

- 正式方案只保存 token 哈希，即 `users.websocket_token_hash`。
- `websocket_token_prefix` 仅用于辅助定位和脱敏展示，不能用于鉴权。
- `websocket_token_last_used_at` 用于记录最近使用时间。
- `websocket_token_rotated_at` 用于记录最近轮换时间。
- `websocket_token_disabled_at` 非空时表示 token 已禁用。
- token 至少使用三十二字节随机值并编码。
- 一个用户一个 token，但允许同一用户多个扩展实例连接，每个连接由 `instanceId` 区分。

### HTTP 与 SSE 鉴权

- C 调用 B 的 HTTP 和 SSE 接口必须鉴权，建议使用 `Authorization: Bearer <api_key>`。
- `api_key` 可复用现有用户体系或后续新增独立 API Key 表，首版方案要求其必须能解析到 `userId`。
- C 的身份需要映射到用户。
- C 只能访问自己名下的工作区和实例。
- 不允许匿名调用 `/api/chat` 和 `/api/chat/stream`。
- B 发送 `server.chat_message` 前必须确认 C 有权操作目标扩展端。
- B 发送 `server.chat_history_request` 前必须确认 C 有权读取目标工作区历史记录。

### 入站消息安全

- `server.chat_message.payload.bypassPromptEnhancement` 固定为 `true`，不允许 C 覆盖为 `false`。
- `autoSend` 需要权限控制，不能默认允许任意 C 自动发送。
- `text` 需要大小限制。
- `dedupeKey` 用于防重复。
- `expireAt` 用于防过期执行。

### 超时策略

- WebSocket 握手超时建议十秒。
- 连接建立后五秒内必须收到 `client.hello`。
- `client.hello` 后十秒内必须收到 `client.connection_context`。
- 扩展端心跳建议三十秒一次，B 连续九十秒未收到任何心跳或业务消息时断开连接。
- 普通 HTTP 请求超时建议一百二十秒。
- SSE 最大生命周期建议十分钟，并支持配置化。
- pending 默认过期时间与普通 HTTP 请求超时一致，即一百二十秒。
- 历史记录 pending 默认过期时间为三十秒。
- pending 清理任务建议每五秒扫描一次。

### 限流策略

- 按 IP 限制 WebSocket 鉴权失败次数，建议每分钟十次。
- 按用户限制 WebSocket 连接数，建议最多二十个。
- 按用户限制 HTTP 请求频率，建议每秒五次。
- 按工作区限制并发 pending 请求数，建议最多八个。
- 历史记录 pending 与模型请求 pending 分桶限流，历史请求不能挤占模型请求并发额度。
- 按连接限制 WebSocket 消息频率，建议每秒一百条。
- 限制 SSE 并发连接数，建议全局最多二百个，并支持配置化。
- 历史记录请求单用户每分钟最多十次，单工作区每分钟最多五次。

### 去重策略

- B 对 A 上报的 `messageId` 做短期去重。
- B 对 C 请求的 `dedupeKey` 做短期去重。
- 同一 `requestId` 下 `eventSeq` 应单调递增。
- 重复的流式片段不应重复写给 C。
- 去重缓存默认过期时间建议五分钟。
- 去重缓存容量上限建议一万条。
- 超出容量后建议按最近最少使用策略淘汰。

### 可观测性

日志建议记录：

- 连接建立和关闭。
- 鉴权成功和失败。
- 握手和连接上下文状态。
- 路由选择。
- 请求开始、完成、取消、错误和超时。
- 历史记录请求开始、成功、失败、超时和响应过大。
- 扩展端断开。
- SSE 断开。
- 限流触发。
- 去重命中。

日志必须脱敏：

- token。
- 数据库密码。
- 用户输入内容默认不记录。
- 工作区路径不记录完整路径。

指标建议：

- 当前 WebSocket 连接数。
- WebSocket 鉴权失败次数。
- HTTP 请求总数。
- 当前 SSE 连接数。
- 当前 pending 请求数。
- pending 超时次数。
- 收发消息总数。
- 丢弃消息总数。
- 重复消息总数。
- 限流次数。
- 请求耗时。
- 历史记录请求总数。
- 历史记录请求超时次数。
- 历史记录请求拒绝次数，按错误码分桶。
- 历史记录响应字节数分布。

## 十四、建议项目结构

```text
.
├── cmd
│   └── server
│       └── main.go
├── internal
│   ├── config
│   │   └── config.go
│   ├── db
│   │   └── mysql.go
│   ├── model
│   │   └── user.go
│   ├── wsserver
│   │   ├── hub.go
│   │   ├── handler.go
│   │   └── protocol.go
│   ├── httpapi
│   │   ├── handler.go
│   │   └── sse.go
│   └── service
│       └── router.go
├── config.yaml
├── go.mod
└── README.md
```

## 十五、实施顺序建议

后续如果开始实施，建议按以下步骤：

1. 初始化 Go 模块。
2. 新增配置读取，并使用环境变量注入数据库密码。
3. 新增 MySQL 连接。
4. 增加用户 token 校验逻辑。
5. 定义统一 `Envelope` 协议结构。
6. 实施 WebSocket 服务和握手流程。
7. 实施连接中心和多索引缓存。
8. 实施连接独立发送队列和串行写协程。
9. 实施 `client.connection_context` 上报和路由注册。
10. 实施 HTTP 鉴权。
11. 实施普通 `POST /api/chat` 转发到 `server.chat_message`。
12. 实施 `POST /api/chat/stream` 和 SSE 事件映射。
13. 实施 `POST /api/chat/history` 和 `server.chat_history_request`。
14. 实施历史记录响应 `client.chat_history_response` 和错误 `client.chat_history_error` 处理。
15. 实施 pending 请求关联、超时和清理。
16. 实施心跳、断开清理和资源释放。
17. 实施限流、去重和消息大小限制。
18. 增加错误处理、日志脱敏和指标。
19. 使用 MCP 执行数据库 DDL，新增 token 哈希字段和唯一索引。
20. 生成第一批 WebSocket token，计算哈希后入库，明文 token 只展示一次。
21. 编写最小测试客户端验证链路。

## 十六、本阶段结论

本阶段只输出方案，不进行代码实施，不修改数据库结构，不新增数据库用户。
